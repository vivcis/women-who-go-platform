export interface NavItem {
  label: string;
  href: string;
  section?: string;
}

export interface CorePillar {
  icon: string;
  title: string;
  description: string;
}

export interface Offering {
  icon: string;
  title: string;
  description: string;
}
